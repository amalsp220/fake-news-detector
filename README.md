# fake-news-detector
End-to-end machine learning system for automatic fake news detection using transformer models (BERT), baseline ML classifiers, and interpretability tools (SHAP/LIME). Includes dataset preprocessing, model training, evaluation, and a Streamlit web interface for real-time predictions with explainability.
